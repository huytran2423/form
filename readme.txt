To run my Spring Boot app from a command line in a Terminal window. You can download the executable jar file docker-spring-boot.jar to your local machine.

Then run this command on the folder you downloaded the file.

java -jar docker-spring-boot.jar 
Note: the java should be 11 or later.

Then open the browser to verify the application at http://localhost:8080